# HTTP/2 Support for @unison/http — Implementation Plan

**Branch:** `http2`  
**Date:** 2026-04-14  
**Status:** In progress — Stages 1–7 complete, post-merge fixes in progress

---

## Completed

- [x] Stage 1: Binary frame codec (`http2-frame.u`)
- [x] Stage 2: HPACK header compression (`http2-hpack.u`)
- [x] Stage 3: Connection state machine (`http2-conn.u`)
- [x] Stage 4: IO layer with multiplexing (`http2-io.u`)
- [x] Stage 4b: Server-side stream acceptance — `IncomingQueue`, `Http2Conn.accept` (`http2-io-server.u`)
- [x] Stage 5: Server integration — `server.http2.{handleConn,runServer,forkServer,serve}` (`http2-server-integration.u`)
- [x] Stage 6: Client integration — `Http2Pool`, `client.http2.{handler,run}` (`http2-client-integration.u`)
- [x] Stage 7: Integration tests (`http2-tests.u`)
- [x] README: `http2.README` and updated top-level `README` (`http2-readme.u`)
- [x] P0: HPACK mutex fix — `respond` and `request` now hold `writeMutex` during encode+send
- [x] P0: SETTINGS propagation — `maxFrameSize` applied via `sendDataFrames` splitting; `headerTableSize` already propagated in `processFrame`
- [x] P1: Auto-connect — `Http2Pool.connect` opens TCP connection and inserts into pool
- [x] P1: Flow control enforcement — `trySendCredit` CAS-decrements both connection and
  stream send windows before each DATA frame; sleeps and retries if window is exhausted;
  DATA frames now sent outside writeMutex (per-frame) so reader loop can deliver
  WINDOW_UPDATE while a large body is in flight; receive-side WINDOW_UPDATE already
  sent in `processFrame`.

---

## Remaining Work

### P1 — Usability (should do before merge)

- [ ] **Auto-connect in `Http2Pool`**: Add `Http2Pool.connect` that opens a TCP connection,
  creates an `Http2Conn`, and inserts it into the pool. Currently callers must do this
  manually. Add reconnect-on-failure logic using `tryScope`.

### P2 — Protocol completeness (nice to have)

- [x] **RST_STREAM send path**: `Http2Conn.cancel sid` sends RST_STREAM (CANCEL) and removes stream promise.
- [x] **GOAWAY on shutdown**: `Http2Conn.close lastStreamId` sends GOAWAY then closes connection.
- [x] **PING keepalive**: Background 30-second PING loop started in `Http2Conn.new`.

### P3 — Runtime dependency (blocked)

- [ ] **ALPN negotiation**: The server currently speaks HTTP/2 on all connections
  regardless of TLS ALPN. Browsers and `curl` won't auto-negotiate. Requires a Unison
  runtime enhancement to expose ALPN selection from `ServerConfig`/`ClientConfig`.
  File a runtime issue requesting:
  - `ClientConfig.alpn.set : [Text] -> ClientConfig -> ClientConfig`
  - `ServerConfig.alpn.set : [Text] -> ServerConfig -> ServerConfig`
  - `Tls.negotiatedProtocol : TlsSocket -> Optional Text`

---

## Order of Attack

1. ~~HPACK mutex~~ ✓
2. ~~SETTINGS propagation (maxFrameSize + headerTableSize)~~ ✓
3. Flow control enforcement
4. Auto-connect in `Http2Pool`
5. `StreamResult` rename
6. RST_STREAM / GOAWAY / PING
7. File ALPN runtime issue

---

## Known Limitations (documented in `http2.README`)

- No ALPN negotiation — static HTTP/2 only
- Flow control tracked but not enforced
- No auto-connect / reconnect in `Http2Pool`
- No WebSocket-over-HTTP/2
